version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      
  pre_build:
    commands:
      - echo "Pre-build phase - checking files"
      - ls -la
      
  build:
    commands:
      - echo "Build phase - preparing static files"
      - echo "HTML and CSS files ready for deployment"
      
  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
  name: website-artifacts
